<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ConvenienceStore.Views.InventoryView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ConvenienceStore.Views"
    xmlns:viewmodels="using:ConvenienceStore.ViewModels"
    xmlns:models="using:ConvenienceStore.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel>
        <TextBlock Text="Quản lý kho hàng" FontSize="24" FontWeight="Bold" Margin="10"/>
        <ListView ItemsSource="{Binding InventoryList}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:ProductModel">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{x:Bind ProductName}" Width="150"/>
                        <TextBlock Text="{x:Bind CategoryName}" Width="100"/>
                        <TextBlock Text="{x:Bind Brand}" Width="100"/>
                        <TextBlock Text="{x:Bind QuantityInStock}" Width="60"/>
                        <TextBlock Text="{x:Bind FormattedPrice}" Width="80"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</Page>
